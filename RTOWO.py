# wegwegtfuwwy and painfuwwy made by Wayback2010
import keyboard, time, uwuify

print('Running...')

while True:
    if keyboard.is_pressed('r'):
        time.sleep(0.05)
        keyboard.press('backspace')
        keyboard.release('backspace')
        keyboard.press('w')
        keyboard.release('w')
        typed=True
    if keyboard.is_pressed('l'):
        time.sleep(0.05)
        keyboard.press('backspace')
        keyboard.release('backspace')
        keyboard.press('w')
        keyboard.release('w')
